import React from "react";

function LoginForm() {
  return (
    <div>
      <h1>Login Form</h1>
      <input type="text" />
      <input type="password" />
      <button>Login</button>
    </div>
  );
}

export default LoginForm;
